# 待办（TheGreatConquest）

本文件汇总当前未落的代码工作。**A. 战斗阶段批次2(引擎层)已全部落地并通过验证**(见下"已完成"说明);**B. 既有的产品层代办** —— 与战斗阶段无关、在 [操作逻辑.md §14.2](操作逻辑.md) 已登记的占位/缺口,仍是当前主要代办面。

> 进度与决策快照见记忆 [thegreatconquest-grill-batch](../memory/thegreatconquest-grill-batch.md)。术语见 [CONTEXT.md](CONTEXT.md),架构决策见 [docs/adr/](docs/adr/)。

---

## A. 战斗阶段 · 批次2(引擎层)—— 已完成 ✅

批次1(数据模型层)与批次2(引擎层)均已落代码并通过验证:
- **验证**:`py smoke_test.py`(exit 0,player 胜)、`py -m unittest discover -s pyconsole/tests`(149 测全过)、`py -m unittest discover -s pydemo/tests`(27 个新测全过)。

### 已实现内容(对照原 A1–A5)

- **A1 主动技能执行**:[battle.py](pydemo/game/battle.py) 行动段按策略表主动区行序检测,首个 `_can_fire_active`(AP+Mana 足、无目标型必要条件满足)的主动技能经 `execute_active_effect` 执行(`ap_damage` 走 `resolve_strike` 统一命中/格挡/暴击管道、`apply_status` 施加状态),扣 AP/Mana;都不满足则普通攻击。
- **A2 被动时点调度**:`dispatch_trigger`(按时点各自计、每时点最多 1 单位响应、速度降序、判满足即扣 PP),10 时点接入循环(`battle_start`/`battle_end` 一次性、`self_attack_*`/`ally_attacked_*` 在 strike 前后、`on_block`/`on_eva`/`after_phys`/`after_magic` 在 `resolve_strike` 内)。
- **A3 8 槽两区策略表 + 条件枚举**:[formation.py](pydemo/game/formation.py) `UnitStrategy`/`StrategyRow`/`validate_strategy`(≤8 行)/`default_strategy`(按 kind 分流主动/被动)/`choose_target_with_slots`(必要先过滤、池空回退不软锁、优先后稳定排序、无条件退回旧 `target_pref`);[triggers.py](pydemo/game/triggers.py) `eval_necessary`/`priority_key`。
- **A4 状态系统(消费模型,无 tick)**:[triggers.py](pydemo/game/triggers.py) `StatusType`/`StatusConsume`/`STATUS_META` + `apply_status`(重施加取 max 不叠加)/`consume_on_self_attack`/`consume_on_self_hit`/`clear_statuses`/`is_frozen`。[battle.py](pydemo/game/battle.py) 接入:进场 `clear_statuses`、冻结跳过出手 + `consume_on_self_attack`、`resolve_strike` 命中分支 `consume_on_self_hit`、`battle_end` 清场所有单位;条件 `enemy_has_frozen`/`pref_enemy_frozen`/`pref_enemy_debuffed` 读 `Unit.statuses`。
- **A5 技能描述面板**:[inventory.py](pydemo/tui/scenes/inventory.py) `_describe_effect` 扩 `ap_damage`/`apply_status`;[unit.py](pydemo/tui/scenes/unit.py) W2 加技能面板(技能名 + kind + AP/PP/Mana 消耗 + 触发时点 + 逐 effect 描述)。

### 数据与测试(本期新增)

- [skills.json](pydemo/data/skills.json):`fireball` 补 `ap_cost:1`;新增 `ice_brand`(主动物伤 30 + 施加冻结 1 层,AP 2)、`frost_warden`(被动 `on_block` 把攻击者冻结 1 层,PP 1)。
- [heroes.json](pydemo/data/heroes.json):`knight` 加 `frost_warden`(让 smoke_test 跑到被动)。
- [pydemo/tests/test_battle_engine.py](pydemo/tests/test_battle_engine.py)(新建,27 测):状态消费模型、策略表上限/fallback、选目标必要过滤/优先排序、格挡减半/闪避零伤/暴击×1.5/开关、火球扣 AP、AP 不足回退普攻、battle_start/end、冻结跳过、无 skill_defs 退化、smoke_test 进程内回归。

### 仍延后(本期降级,非阻塞)

- **A6 HeroDef.map_skills 接逻辑**:值仍为 `[]`,语义为地图级战略技能(行军加速/侦察/瞬移),接战略层而非战斗层。属后续产品扩展。
- **A7 AI 使用装备**:[scenario.py](pydemo/cli/scenario.py) 只给玩家发装备,AI 原型不使用装备。属后续产品扩展或明确记为"AI 不用装备"的设计选择。

### 格挡/闪避掷骰(本期新增承重语义)

为让 `on_block`/`on_eva` 能真正触发(凑齐 10 时点),`resolve_strike` 落地命中/格挡/闪避/暴击掷骰(单一管道,普通攻击与 `ap_damage` 共用)。模块开关 `BLOCK_EVA_ENABLED`(默认 True)与 `BLOCK_DMG_FACTOR=0.5`:若 smoke_test 胜负翻转可一键关掷骰或调系数,不改代码。详见 [ADR-0011](docs/adr/0011-passive-trigger-points-and-strategy-conditions.md)。

---

## B. 既有产品层代办(非战斗阶段,详见 [操作逻辑.md §14.2](操作逻辑.md#L719-L732))

1. **普通兵招募动作(最高优先级)**:业务层仅有 `action_recruit_hero`,无 `action_recruit_unit`(据点 + 兵种 → 招普通兵)。现状靠 [scenario.py](pydemo/cli/scenario.py#L123-L132) 开局硬塞预备兵兜底。需新增 `Game.action_recruit_unit` + AI 接此动作 + UI 入口,使兵源可持续;**完成后移除 scenario.py 的初始预备兵兜底**。
2. **领主(Lord)重定义**:建造改为即时(ADR-0006)后,领主"建造时间 -1 回合"失效,`player.lords` 保留为占位,职责待重定义或移除。
3. **意志(Will)职责重定义**:`will` 属性暂保留并参与增长(增长率 0.3),但其"决定部队规模"职责已移交领导力,具体用途待重定义。
4. **标志性建筑命名表**:据点总览"标志建筑"列直接显示 `landmark_type` 字符串(weak/medium/strong)作占位,后续加命名表替换为中文名。
5. **存档/读档**:`Game.snapshot()/restore()` 完整序列化留待业务层扩展;当前退出选单的保存/读取仅记录天数(占位)。
6. **外交系统**:业务层无外交系统,外交界面(§4)为占位,仅保证 `J` 键可用。
7. **科技/文化系统**:业务层无科技/文化系统,学习记录保存在 TUI 控制器、资源在 TUI 侧扣除,不影响战斗与经济(占位)。
8. **指派领主接口**:指派领主用直接 `player.lords[sh.id]` 写入,无业务层接口(占位)。
9. **聚贤庄→待命池命名迁移**:`hall.py` 场景类已改名 `StandbyScene`,但文件名仍为 `hall.py`,待后续整体重命名。
10. **装备获取渠道**:仓库目前仅靠 scenario.py 开局发 3 件/定义,无战斗掉落/事件/购买等持续获取机制,后续需补。

---

## 验证约定

- 战斗引擎改动以三套验证全过为准:`py smoke_test.py`(exit 0 = 过)、`py -m unittest discover -s pyconsole/tests`(现有 149 测不破)、`py -m unittest discover -s pydemo/tests`(战斗行为单测,现 27 测)。
- 战斗日志在 `battle.py` 的 `result.log` 中,smoke_test 走 GBK 终端中文会乱码但可读,以 exit=0 与胜负结果为准。
- 若 smoke_test 胜负翻转,先动 `battle.py` 的 `BLOCK_EVA_ENABLED` 或 `BLOCK_DMG_FACTOR` 调参,不改代码。
